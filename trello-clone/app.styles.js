import { makeStyles } from '@material-ui/styles';

const useAppStyles = makeStyles({
  App: {
    display: 'block',
    width: '100%',
    overflow: 'auto'
  },
  container: {
    position: 'relative'
  }
});

export default useAppStyles;
